# rest-client

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rest-client` to build the library.

## Build client from OpenAPI

Run `nx generate-client rest-client` to build the client from OpenAPI.

[Platformatic client](https://docs.platformatic.dev/docs/client/frontend)

## Running unit tests
Run `nx test rest-client` to execute the unit tests via [Node.js](https://nodejs.org).